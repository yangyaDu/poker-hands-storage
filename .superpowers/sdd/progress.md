# V3 release-gate execution ledger

- Task 1: completed — restored protobuf build contract and formatting baseline; `proto_v3_format_test` passes.
- Task 2: completed — export now runs complete standalone read-back verification after writing the building manifest and before publication; verification failure returns `VERIFY_ERROR` and removes the sibling `.building` directory. TDD evidence: red `cargo test -p poker-hands-storage-tools --lib export_failure_during_read_back_verification_removes_building_without_publishing` failed with missing `export_v3_archive_with_verifier`; green the same command passed (1/1), and `cargo test -p poker-hands-storage-tools --test proto_v3_verification_test` passed (4/4).
- Task 3: completed — `V3FacadeOptions` metadata/strategy byte budgets are facade-wide totals dynamically divided by the current open-handle count (`total / N`, `N >= 1`), rather than statically reserved by capacity. Opening a dimension resizes every retained metadata and strategy cache; `ByteLru` evicts LRU entries until it fits the new budget. An evicted handle is first resized to zero so an in-flight query cannot retain an unaccounted decoded cache. `FacadeCacheStats` exposes the actual current per-handle sub-budgets and aggregate resident bytes. Service configuration and README/Docker documentation preserve the global-budget meaning without claiming a shared decoded cache. TDD evidence: red `cargo test -p poker-hands-storage-tools --test proto_v3_query_test facade_total_cache_budgets_cover_two_open_dimension_handles --target x86_64-pc-windows-msvc` failed because the new observable per-handle budget fields did not exist; green passed after implementation. The regression asserts one handle gets the full total, two handles get half each, a pre-existing decoded strategy is LRU-evicted on shrink, and aggregate resident bytes stay within each facade total. Final verification: `cargo fmt --all -- --check` passed; full V3 query suite passed (6/6); service config suite passed (3/3); `cargo check --workspace --target x86_64-pc-windows-msvc` passed.
- Task 4: completed — release build passed (`cargo build -p poker-hands-storage-tools --release`). The initial `data/proto-v3-releases/2026-07-17T000000Z` export was stopped by the 60-second foreground execution limit and is preserved untouched as an incomplete root. Authorized replacement `data/proto-v3-releases/2026-07-17T000001Z` was exported with `v3-export-all --source data/sqlite/range.db --out-root ...` in a retained background process (exit 0; `reports/v3-release-20260717/export-all-attempt3.log` records all 9 archives exported and internally cross-verified). Dimensions: `default_{6,8,9}max_{100,200,300}BB`; each contains all 7 required files. Per-dimension standalone reports `reports/v3-release-20260717/verify-<dimension>.json`: 9/9 `ok=true`, `failureCount=0`. SQLite reports `reports/v3-release-20260717/cross-<dimension>.json`: 9/9 `ok=true`, `failureCount=0`, `mappingDifferences=actionDifferences=cellDifferences=0`. Release benchmark reports `reports/v3-release-20260717/benchmark-<dimension>.json` and `.md`, each run with `--iterations 1000 --warmup-iterations 50`: 9/9 `correctnessVerified=true`, P50/P95, cache statistics, and before/after RSS present. Final aggregate report check validated all 36 JSON/Markdown gate artifacts; no Task 4 failures.
- Task 5: completed — aligned V3 plan, Proto index, root/docs maps, verification/benchmark guide, runtime operations, and Docker deployment guide to the 2026-07-17 release gate. The immutable verified root is `data/proto-v3-releases/2026-07-17T000001Z`; `reports/v3-release-20260717/` contains 9/9 zero-failure standalone reports, 9/9 zero-difference SQLite cross reports, and 9/9 `correctnessVerified=true` benchmark reports. Follow-up doc review corrected the remaining stale roadmap and root/Proto wording: V3 is the primary release format, service/native runtime reads V3 only, and `storage-tools` retains explicit V2 reference commands rather than implying an argument-free V3 CLI default. V2 dual-read or fallback remains prohibited.
- Task 6A: completed — introduced the private `v3::storage_file` module as the internal seam for
  staged data/index file pairs, fixed-header protobuf payload writing, fixed-width index sections,
  and manifest file hashing. `MetadataPaths` now names the drill-scenarios and abstract-action-paths
  pairs instead of indexing `[PathBuf; 4]`; `HandStrategyStore` reuses the same staged publication
  and writers. The public `MetadataStore`, `HandStrategyStore`, and export options did not change;
  V3 on-disk bytes, headers, sections, CRCs, and manifest contents remain unchanged. Baseline and
  final evidence: `cargo test -p poker-hands-storage-tools --test proto_v3_format_test --test
  proto_v3_metadata_test --test proto_v3_hand_strategy_test` passed (15/15), `cargo fmt --all --
  --check` passed, and `git diff --check` passed.
- Task 6B: completed — extracted V3 metadata source loading, protobuf pagination, named staged
  file publication, and metadata data/index write orchestration into the private
  `v3::metadata_export` module. `MetadataStore` remains the reader/query/verification module;
  it re-exports the established metadata export interface so archive callers and tests keep their
  existing paths. Canonical hash-locator ordering and checked fixed-width index conversion now
  live in `v3::format`, which both implementations use without a reverse reader-to-export
  dependency. No V3 schema, disk bytes, or query semantics changed. Baseline metadata suite
  passed (6/6); final V3 format, metadata, query, and verification suites passed (22/22), and
  `cargo fmt --all -- --check` plus `git diff --check` passed.
